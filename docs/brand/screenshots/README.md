# Screenshots

Release-facing screenshots used by the root README.

Capture screenshots from a clean local WebUI instance or committed product preview routes. Do not include personal runtime history, provider keys, local paths, or private generated outputs.

The README should show real product workspace content when possible, not only empty shell states.
